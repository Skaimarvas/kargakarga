import React from "react";

const Header = () => {
  return (
    <nav className="fixed flex gap-2 right-4 top-4">
      <span className="text-[#145389] font-fortuna font-bold">kargakarga</span>
    </nav>
  );
};

export default Header;
